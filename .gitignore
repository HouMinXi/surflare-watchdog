# Runtime lock file (created in /run, not tracked)
*.lock
.idea/
.worktrees/
__pycache__/
REVIEW.md
# Root-level route files are runtime copies from /etc/surflare/; baseline lives in routes/
cn_ipv4.txt
cn_ipv6.txt

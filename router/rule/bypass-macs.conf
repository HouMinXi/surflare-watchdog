# bypass-macs.conf -- devices that bypass tproxy (use CN ISP IP directly)
# One MAC per line, # lines are comments
# IPs are auto-resolved from /tmp/dhcp.leases on each VPN connect
#
# Rule mode: surflare-proxy Smart Routing handles CN/non-CN split at the
# application layer, so per-device bypass is unnecessary.  All LAN devices
# get CN direct + non-CN VPN through the proxy.
#
# AnyConnect devices are auto-detected via DTLS (auto_bypass set).
#
# Leave this file empty (comments only) in rule mode.

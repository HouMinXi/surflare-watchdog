# Disable 802.11ax (HE) to work around iwlmld firmware beacon timing bug
# (upstream Bug 203709 — OPEN, Intel firmware, affects kernel 5.x–6.19+).
# Falls back to VHT (802.11ac / ~867 Mbps on 5 GHz 80 MHz).
options iwlwifi disable_11ax=1 power_save=0
